# Core dependencies — mirrors [project].dependencies in pyproject.toml
# Install: pip install -r requirements.txt
# (uv users: uv sync  or  uv pip install -r requirements.txt)

fastapi>=0.103.0
uvicorn>=0.23.2
sqlalchemy>=2.0.20
pydantic>=2.3.0
ollama>=0.1.0
pandas>=2.1.0
python-dotenv>=1.0.0
langgraph>=1.1.3
langchain-ollama>=1.0.1
langchain-community>=0.4.1
pyyaml>=6.0
ruff>=0.15.8

# Dev / test dependencies — mirrors [dependency-groups].dev
pytest>=7.4.0
pytest-mock>=3.12.0
httpx>=0.25.0
tabulate>=0.9.0
streamlit>=1.35.0
requests>=2.31.0
